/* selectors */
export const getAllTabs = ({ tabs }) => tabs;

/* reducer */
export default function reducer(statePart = [], action = {}) {
  switch (action.type) {
    default:
      return statePart;
  }
}
